# eslint-plugin-jsx-a11y (latest: 6.10.2) caps its eslint peer range at ^9 while
# the repo is on eslint 10; the plugin's flat config works fine on 10. Without
# this, npm ci / install fail on that peer conflict. Drop once jsx-a11y ships an
# eslint-10 peer range.
legacy-peer-deps=true
